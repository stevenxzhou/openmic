name: Build and deploy docker image in dev

on:
  workflow_run:
    workflows: ["Version Bump"]
    types: [completed]
    branches: [main]
jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Docker registry
        uses: docker/login-action@v3
        with:
          registry: stevenxzhou.com:5500
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Read app version
        id: app_version
        run: |
          VERSION=$(node -p "require('./package.json').version")
          echo "version=$VERSION" >> $GITHUB_OUTPUT

      - name: Build and Deploy
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./Dockerfile
          push: true
          build-args: |
            NEXT_PUBLIC_BASE_PATH=/openmic-dev
            NEXT_PUBLIC_NODE_ENV=development
          tags: |
            stevenxzhou.com:5500/openmic-dev:latest
            stevenxzhou.com:5500/openmic-dev:${{ steps.app_version.outputs.version }}
          platforms: linux/amd64
